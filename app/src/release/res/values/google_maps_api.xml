<resources>
    <!--
    google_maps_key is provided at build time from local.properties (MAPS_API_KEY).
    See local.properties.example in the project root.

    For release, add your release keystore SHA-1 to the same (or a separate) restricted key
    in Google Cloud Console. Package name: com.evya.weatherapp
    -->
</resources>
